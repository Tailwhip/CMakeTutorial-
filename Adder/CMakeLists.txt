# Name of the library building along with the project and it's source files.
# This CmakeLists.txt file have to be created for every library building with the project.
add_library(adder adder.cpp adder.h)